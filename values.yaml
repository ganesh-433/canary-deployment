# This file defines the values for our Kubernetes manifests.
image:
  repository: ganesh243/canary-app
  # This is the corrected expression.
  # It points to the tag of the "primary" artifact configured in the Harness service.
  tag: "<+artifacts.primary.tag>"
